services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: raijin
      POSTGRES_PASSWORD: raijin
      POSTGRES_DB: raijin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
